# Pin ranges are conservative; run `pip install -r requirements.txt` to resolve.
# Verify latest compatible versions if you hit a resolution error.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
httpx>=0.27,<1.0
python-multipart>=0.0.9
